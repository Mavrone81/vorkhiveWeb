// Language overrides merged over the English defaults at render time.
// AI-generated translations — recommend a native-speaker review before relying
// on them for marketing. Any field omitted here falls back to English.
import zh from './zh.js';
import ms from './ms.js';
import ta from './ta.js';
import th from './th.js';

export const translations = { zh, ms, ta, th };
